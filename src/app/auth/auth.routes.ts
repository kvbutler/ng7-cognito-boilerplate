import { Routes } from '@angular/router';
import { CodeCallbackComponent } from './codeCallback.component';

export const AuthRoutes: Routes = [
  { path: 'code', component: CodeCallbackComponent }
];
